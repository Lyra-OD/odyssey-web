/** Courbe signature Quiet Luxury — partagée par les panneaux et micro-interactions. */
export const EASE_OUT_LUXE = [0.16, 1, 0.3, 1] as const;

/** Ouverture organique (bottom sheet, focus, reveal formulaire) — 320 ms. */
export const DURATION_BREATH = 0.32;

/** Transition rituelle (overlays, changements d’étape Sanctuaire) — 480 ms. */
export const DURATION_RITUAL = 0.48;
