# Constellation orbit experiments (Acte I — abandonné)

Essais « GPS / anneaux » autour de la Lueur — non branchés dans le ciel actif.

| Fichier | Rôle |
|---------|------|
| `OrbitalRings.tsx` | Sillons fixes (Line) |
| `ConstellationOrbit.tsx` | `StarOnOrbit` — étoile sur cercle |
| `GravityBridges.tsx` | Filaments Lueur → étoiles |

**Actif :** reset orb-cloud dans `SanctuaryUniverse` (sans drag).

Restaurer : recopier vers `constellation/` et rebrancher manuellement.
