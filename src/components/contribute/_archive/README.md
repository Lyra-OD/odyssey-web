# Archive — essais ciel pré-WebGL

Prototypes non branchés. Le ciel actif est `SanctuaryUniverse` + `StarDust` / `NebulaGas`.

- `SanctuaryCanvasSky.tsx` — canvas 2D + orbes vidéo
- `SanctuaryLueurSky.tsx` — layout DOM de LueurOrbs
