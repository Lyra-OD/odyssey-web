# Archive — Constellation « orb cloud » V1

Snapshot avant **Éclipse Résonnante / Acte I** (anneaux gravitationnels).

**Ciel inchangé** (NebulaGas, CosmicDust, StarDust, ShootingStars) — seule la couche constellation évolue.

## Contenu
- `mockSouls.ts` — positions libres / asymétriques
- `LightBridges.tsx` — lignes droites hero → satellites
- `LueurNode.tsx` — tailles / teintes au moment du snapshot
- `SanctuaryUniverse.snapshot.tsx` — Constellation + drag + reveal E inclus

## Restaurer
1. Recopier `mockSouls.ts` → `constellation/mockSouls.ts`
2. Recopier `LightBridges.tsx` → `constellation/LightBridges.tsx`
3. Recopier `LueurNode.tsx` → `contribute/LueurNode.tsx`
4. Dans `SanctuaryUniverse.tsx`, remettre la logique `Constellation` depuis le snapshot (ou restaurer le fichier entier si tu n’as rien d’autre à garder)

Date snapshot : 2026-08-05
